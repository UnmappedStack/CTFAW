extern puts(txt: u64);
const globalthing: u32 = 21 + 3 * 2;

fn main() -> u32 {
    let msg: char* = "hello world from the libc!\n";
    puts(msg as u64);
    let num: u32 = 5;
    // write the text to stdout
    let num_addr: u32* = &num;
    *num_addr = 42;
    return num + globalthing;
}
