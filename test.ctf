func main() {
    let msg: u64 = "hello world!\n";
    // write the text to stdout
    asm("mov rax, 1\nmov rdi, 1\nmov rdx, 13\nsyscall" : "rsi" | msg : : );
    return 42;
}

func _start() {
    let status: u64 = main();
    asm("mov rax, 60\nsyscall" : "rdi" | status : :);
}
